// compact memory
// update partitions
// generate simulaiton with tha compacted memory
// generate conditions to first proccess
// execute the first procees (returns a simulation)
// join simulation in simulations

function second() {
  // compact memory
  // update partitions
  // generate simulaiton with tha compacted memory
  // generate conditions to first proccess
  // execute the first procees (returns a simulation)
  // join simulation in simulations
  // return list of simulations
}



// function isAssignable(partitions, sizeWork, typeMemory) {
//   const freePartitions = existsFreePartitions(partitions);
//   return freePartitions
//     ? managementConditions(sizeWork, freePartitions, typeMemory)
//     : false;
// }