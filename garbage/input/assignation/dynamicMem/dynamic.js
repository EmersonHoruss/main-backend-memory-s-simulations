function assignationDynamicMemory(
  assignation,
  event,
  memory,
  partitions,
  work,
  typeMemory
) {
  ({ condition, result } = assignation);
  const workSize = work.size;
  if (condition === "first") {
  } else if (condition === "second") {
  }
}

function dynamic(params) {
  if (condition === "first") {
  } else if (condition === "second") {
  }
}
